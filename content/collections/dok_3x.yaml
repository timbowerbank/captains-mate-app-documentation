title: 'Dok 3.x'
icon: programming-code-block
template: docs/show
layout: docs/layout
revisions: false
route: "{{ if !parent_uri }}dok/3.x/{{ /if }}{{ parent_uri ?= '{parent_uri}/' }}{{ slug }}"
sort_dir: asc
date_behavior:
  past: null
  future: null
preview_targets:
  -
    label: Entry
    url: '{permalink}'
    refresh: true
structure:
  root: true
